# =============================================================================
# i3wm 配置文件
# =============================================================================

# =============================================================================
# §1. 变量定义 (Variables)
# =============================================================================
# 定义 Mod 键 (通常是 Win 键) 和 Alt 键
set $mod Mod4
set $alt Mod1

# 定义工作区名称，方便后续引用
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# =============================================================================
# §2. 外观设置 (Appearance)
# =============================================================================
# 窗口标题和 i3bar 的默认字体
font pango:Noto Sans Mono 16

# 窗口边框和标题栏颜色
# class                 border  bground text    indicator child_border
client.focused          #FF79A4 #6272A4 #F8F8F2 #6272A4   #FF79A4
client.focused_inactive #44475A #44475A #F8F8F2 #44475A   #44475A
client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #282A36
client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36
client.background       #F8F8F2

# 窗口边框样式
# 所有窗口使用 2 像素的边框
for_window [class="^.*"] border pixel 2
# 新建的平铺窗口无边框
new_window none
# 新建的浮动窗口使用 normal 样式的边框
new_float normal
# 当窗口接触屏幕边缘时隐藏边框 (例如全屏时)
hide_edge_borders both

# i3-gaps 设置
gaps inner 3
gaps outer 3
smart_gaps on
smart_borders on

# =============================================================================
# §3. 行为设置 (Behavior)
# =============================================================================
# 焦点跟随鼠标移动
focus_follows_mouse yes

# 当焦点转移时，鼠标指针不自动移动到新窗口的中心
mouse_warping output

# 使用 Mod 键 + 鼠标左键拖动浮动窗口
floating_modifier $mod

# =============================================================================
# §4. 自启动程序 (Autostart)
# =============================================================================
# 启动 fcitx5 (中文输入法框架)
exec --no-startup-id fcitx5
# 启动 flameshot (截图工具)
exec --no-startup-id flameshot &
# 通知服务
exec --no-startup-id /usr/lib/xfce4/notifyd/xfce4-notifyd 
# 显示器设置脚本
exec --no-startup-id ~/.config/i3/monitor_settings.sh
# 启动 Polybar
exec_always --no-startup-id LC_ALL=C ~/.config/polybar/launch.sh 
# 启动 Picom (窗口合成器，用于透明等特效)
exec_always --no-startup-id picom --config ~/.config/picom/picom.conf
# 启动 Variety (壁纸管理器)
exec --no-startup-id variety
# 启动 Unclutter (超时后自动隐藏鼠标指针)
exec_always --no-startup-id unclutter --timeout 3 --jitter 10 --ignore-scrolling
# 启动 Conky
exec_always --no-startup-id ~/.config/i3/conky.sh 

exec_always --no-startup-id ~/.config/i3/blueberry.sh

# =============================================================================
# §6. 快捷键绑定 (Keybindings)
# =============================================================================

# --- 应用程序启动 (Application Launchers) ---
# 启动终端 (Alacritty)
bindsym $mod+Return exec --no-startup-id alacritty;focus
# 启动一个不透明的终端
bindsym $mod+Shift+Return exec --no-startup-id alacritty --config-file ~/.config/alacritty/notransparent.toml;focus
# 启动 Chrome
bindsym $mod+c exec LANGUAGE=zh_CN google-chrome-stable
# bindsym $mod+c exec firefox
# 启动 VS Code
bindsym $mod+z exec code
# 启动 dmenu (程序启动器)
bindsym $mod+d exec "dmenu_run -i -nf '#F8F8F2' -nb '#282A36' -sb '#6272A4' -sf '#F8F8F2' -fn 'monospace-14' -p 'dmenu%'"
# 启动 Rofi
bindsym $mod+Shift+d exec --no-startup-id "rofi -show ts -modi ts"
# 启动自定义启动器脚本
bindsym $alt+Shift+d exec --no-startup-id ~/.config/i3/launcher/launcher.sh
# 启动 Flameshot GUI 截图
bindsym $alt+Control+a exec --no-startup-id LANGUAGE=zh_CN flameshot gui
# 启动 XFCE 设置管理器
bindsym $mod+Shift+x exec --no-startup-id xfce4-settings-manager

# --- 窗口管理 (Window Management) ---
# 关闭当前窗口
bindsym $mod+Shift+q kill
# 窗口聚焦 (使用 Vim 风格的 hjkl 键)
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# 移动窗口 (使用 Vim 风格的 hjkl 键)
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
# 水平分割
bindsym $mod+semicolon split h
# 垂直分割
bindsym $mod+v split v
# 全屏切换
bindsym $mod+f fullscreen toggle
# 切换窗口布局 (stacking, tabbed, split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
# 浮动/平铺切换
bindsym $mod+Shift+space floating toggle
# 在浮动和平铺窗口间切换焦点
bindsym $mod+space focus mode_toggle
# 聚焦到父容器
bindsym $mod+a focus parent

# --- 工作区切换 (Workspace Switching) ---
# 切换到指定工作区
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
# 将当前窗口移动到指定工作区
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev

# --- 系统控制 (System Controls) ---
# 屏幕亮度调节
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +3%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 3%-
# 音量调节
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
# 媒体播放器控制
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# --- i3 控制 (i3 Controls) ---
# 重新加载配置文件
bindsym $mod+Shift+c reload
# 重启 i3
bindsym $mod+Shift+r restart
# 退出 i3
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m '您确定要退出 i3 吗？' -B '是的, 退出' 'i3-msg exit'"
# 登出菜单
bindsym $mod+X exec --no-startup-id archlinux-logout

# =============================================================================
# §6. 模式定义 (Modes)
# =============================================================================

# --- 窗口大小调整模式 (Resize Mode) ---
# 进入 resize 模式
bindsym $mod+r mode "resize"
mode "resize" {
    # 使用 hjkl 调整窗口大小
    bindsym h resize shrink width 5 px or 5 ppt
    bindsym j resize grow height 5 px or 5 ppt
    bindsym k resize shrink height 5 px or 5 ppt
    bindsym l resize grow width 5 px or 5 ppt

    # 按下 Enter, Escape, 或 $mod+r 返回默认模式
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

# --- Gaps 调整模式 (Gaps Mode) ---
set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
    bindsym o mode "$mode_gaps_outer"
    bindsym i mode "$mode_gaps_inner"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

mode "$mode_gaps_inner" {
    bindsym plus gaps inner current plus 5
    bindsym minus gaps inner current minus 5
    bindsym 0 gaps inner current set 0
    bindsym Shift+plus gaps inner all plus 5
    bindsym Shift+minus gaps inner all minus 5
    bindsym Shift+0 gaps inner all set 0
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

mode "$mode_gaps_outer" {
    bindsym plus gaps outer current plus 5
    bindsym minus gaps outer current minus 5
    bindsym 0 gaps outer current set 0
    bindsym Shift+plus gaps outer all plus 5
    bindsym Shift+minus gaps outer all minus 5
    bindsym Shift+0 gaps outer all set 0
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# =============================================================================
# §8. 窗口规则 (Window Rules)
# =============================================================================
# 自动聚焦到有紧急提示的窗口
for_window [urgent=latest] focus

# 针对特定应用的窗口规则 (自动聚焦或设为浮动)
for_window [class="Vivaldi-stable"] focus
for_window [class="Spotify"] focus
for_window [class="Thunar"] focus
for_window [class="Gnome-terminal"] focus
for_window [class="Termite"] focus
for_window [class="Terminator"] focus
for_window [class="Urxvt"] focus
for_window [class="Alacritty"] focus

for_window [class="Arcolinux-welcome-app.py"] floating enable
for_window [class="Arcolinux-calamares-tool.py"] floating enable
for_window [class="Bleachbit"] floating disable
for_window [class="Blueberry.py"] floating enable
for_window [class="Brasero"] floating disable
for_window [class="Galculator"] floating enable
for_window [class="Gnome-disks"] floating disable
for_window [class="^Gnome-font-viewer$"] floating enable
for_window [class="^Gpick$"] floating enable
for_window [class="Hexchat"] floating disable
for_window [class="Imagewriter"] floating enable
for_window [class="Font-manager"] floating enable
for_window [class="Nitrogen"] floating disable
for_window [class="Pavucontrol"] floating disable
for_window [class="Peek"] floating enable
for_window [class="^Skype$"] floating enable
for_window [class="^Spotify$"] floating disable
for_window [class="System-config-printer.py"] floating enable
for_window [class="Unetbootin.elf"] floating enable
for_window [class="Usb-creator-gtk"] floating enable
for_window [class="^Vlc$"] floating disable
for_window [class="Wine"] floating disable
for_window [class="Xfburn"] floating disable
for_window [class="Xfce4-appfinder"] floating enable
for_window [class="Xfce4-settings-manager"] floating disable
for_window [instance="script-fu"] border normal
for_window [instance="variety"] floating disable
for_window [title="Copying"] floating enable
for_window [title="Deleting"] floating enable
for_window [title="Moving"] floating enable
for_window [title="^Terminator Preferences$"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="^Preferences$"] floating enable
for_window [window_role="setup"] floating enable

